
from setuptools import setup,find_packages

setup(
    name="logging_util",
    version="1.0.0",
    description="Reusable python logging setup utility",
    author="afreed",
    packages = find_packages(),
    python_requires = ">=3.7",
)

